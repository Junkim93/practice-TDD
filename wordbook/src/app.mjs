import {
  saveInputWord,
  loadWordList,
  getWordList
} from './logics/dataService.mjs'

import inputWordView from './views/inputWordView.mjs'

export const app = {
  saveInputWord,
  loadWordList,
  getWordList,
  inputWordView
}
